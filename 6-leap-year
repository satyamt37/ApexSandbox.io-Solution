public Boolean isLeapYear(Integer year) {
   if ((Math.mod(year,4) == 0) && ((year % 100 != 0) || (Math.mod(year, 100) == 0))) {
        return true; // It's a leap year
    } else {
        return false; // It's not a leap year
    }
}
